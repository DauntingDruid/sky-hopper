import React from 'react'

const SearchBar = () => {
  return (
    <div className='w-full rounded-full bg-gray-200 opacity-95 h-16   hover:border-solid hover:border-2 hover:border-white transition-all ease-in-out duration-300'>
        {/* from -> input autocomplete */}
        {/* to -> input autocomplete */}
        {/* departure -> calendar */}
        {/* passengers -> buttons - + */}
    </div>
  )
}

export default SearchBar