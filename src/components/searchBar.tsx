import React from 'react'

const SearchBar = () => {
  return (
    <div className='w-screen bg-slate-200 h-[35vh]'>
        search
        {/* bg */}
        {/* searchbar -> from , to , date, passengers */}
    </div>
  )
}

export default SearchBar